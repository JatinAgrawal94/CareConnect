import 'package:flutter/material.dart';

class ExamNewComponent extends StatelessWidget {
  const ExamNewComponent({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
